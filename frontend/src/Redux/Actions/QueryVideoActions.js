import { createAction } from '@reduxjs/toolkit'

export const getVideoAction = createAction('GET_VIDEOS') 
export const getSuggestion = createAction('GET_SUGGESTION')
export const sendImg = createAction('SEND_IMG')